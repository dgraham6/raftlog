#include <catch2/catch_test_macros.hpp>

#include "raftlog/raft/raft_node.hpp"

TEST_CASE("scaffold compiles", "[raft]") {
    // Phase 4: replace with a fake Transport and real election/replication
    // tests (dropped/delayed messages, leader crash mid-replication, etc).
    REQUIRE(true);
}
